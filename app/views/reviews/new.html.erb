<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, placeholder: "Write your review here" %>
  <%= f.input :rating, hint: 'A score from 0 to 5' %>
  <%= f.submit "Add review", class: "btn btn-primary" %>
<% end %>
